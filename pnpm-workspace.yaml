packages:
  - "apps/*"
  - "packages/*"

# drizzle-kit ships esbuild and needs its postinstall to place the binary.
onlyBuiltDependencies:
  - esbuild

ignoredBuiltDependencies:
  - sharp
  - unrs-resolver
