# Neon Postgres — copy to .env.local and fill in.
# .env.local is gitignored; never commit real credentials.
DATABASE_URL="postgresql://user:password@host.neon.tech/dbname?sslmode=require"

# Set by CI, not needed locally.
# DOCKERHUB_USERNAME=
# DOCKERHUB_TOKEN=
# RENDER_DEPLOY_HOOK=
